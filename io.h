// Loads source code from file specified in command line arguments
char* load(int argc, char* argv[]);

// Saves output assembly to file with the same name as the input file
void save(int argc, char* argv[], char* src);
